# AI Assistance Disclosure

I used **Cursor (AI-assisted coding)** for this assessment in a limited way:

- **Dummy data** — seed script and synthetic employee/salary records (~10k rows for demo)
- **Some fixes** — small bug fixes and tweaks during development (e.g. API errors, form behavior)

Everything else was built and reviewed by me. Employee and salary data are **not real**; they come from `apps/api/prisma/seed.ts` and `seed-data.ts`.
